# General parameters
params:
   name: Warehouse
   inflation_radius: 0.1  # Collision padding radius around rooms/hallways/locations
   object_radius: 0.1  # Collision padding radius around objects
   wall_height: 1  # Vertical (Z) height of walls, can override with individual entities
